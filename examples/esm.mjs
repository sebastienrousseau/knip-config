/**
 * Native ES Module usage example for @sebastienrousseau/knip-config
 */
import config from "../index.mjs";

console.log("=== ESM Usage Example: @sebastienrousseau/knip-config ===");
console.log("Loaded via ESM default export:", typeof config);
